# in_array()

```php
<?php

// Função in_array($valor, $array)
// A função in_array retorna um valor booleano que indica
// se um valor existe em $array.

echo in_array('oi', ['tudo bem']); // --> false 
echo in_array('ok', ['tudo bem', 'ok']); // --> true
```