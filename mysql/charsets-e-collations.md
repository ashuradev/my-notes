# Charset e collations

Um `charset` é um conjunto de caracteres que o MySQL pode armazenar. E uma `collation set` define como `strings` são ordenadas (se uma letra X vem primeiro que uma letra Y, etc).